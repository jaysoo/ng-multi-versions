import { Component } from '@angular/core';

@Component({
  selector: 'multi-versions-root',
  template: `<router-outlet></router-outlet>`,
  styles: [],
})
export class AppComponent {
  title = 'demo';
}
